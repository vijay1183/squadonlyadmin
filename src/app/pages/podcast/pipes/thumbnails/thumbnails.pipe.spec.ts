import { ThumbnailsPipe } from './thumbnails.pipe';

describe('ThumbnailsPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailsPipe();
    expect(pipe).toBeTruthy();
  });
});
