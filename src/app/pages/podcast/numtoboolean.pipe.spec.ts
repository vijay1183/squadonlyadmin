import { NumtobooleanPipe } from './numtoboolean.pipe';

describe('NumtobooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new NumtobooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
